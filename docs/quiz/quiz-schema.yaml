definitions:
  quizzes:
    type: array
    items:
      type: object
      properties:
        _id:
          type: string
        title:
          type: string
        items:
          type: array
          ref: '#/components/question'
        author:
          type: string
          ref: '#/components/user'
        category:
          type: string
          ref: '#/definitions/resourcesCategory'
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
  quizBody:
    type: object
    properties:
      title:
        type: string
      category:
        type: string
        ref: '#/definitions/resourcesCategory'
      items:
        type: array
        ref: '#/components/question'
  quiz:
    type: object
    properties:
      _id:
        type: string
      title:
        type: string
      items:
        type: array
        ref: '#/components/question'
      author:
        type: string
        ref: '#/components/user'
      category:
        type: string
        ref: '#/definitions/resourcesCategory'
      createdAt:
        type: string
        format: date-time
      updatedAt:
        type: string
        format: date-time
