definitions:
  admin-invitations:
    type: array
    items:
      type: object
      properties:
        _id:
          type: string
        email:
          type: string
        dateOfInvitation:
          type: string
          format: date-time
  admin-invitation:
    type: object
    properties:
      _id:
        type: string
      email:
        type: string
      dateOfInvitation:
        type: string
        format: date-time
  send-admin-invitations:
    type: object
    properties:
      emails:
        type: array
        items:
          type: string
