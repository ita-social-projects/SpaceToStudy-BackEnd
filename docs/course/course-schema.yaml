definitions:
  courses:
    type: array
    items: 
      type: object
      properties:
        _id:
          type: string
        title:
          type: string
        description:
          type: string
        author:
          type: string
          ref: '#components/user'
        lessons:
          type: array
          items:
            type: string
            ref: '#components/lesson'
        attachments:
          type: array
          items:
            type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
  course:
    type: object
    properties:
      _id:
        type: string
      title:
        type: string
      description:
        type: string
      author:
        type: string
        ref: '#components/user'
      lessons:
        type: array
        items:
          type: string
          ref: '#components/lesson'
      attachments:
        type: array
        items:
          type: string
      createdAt:
          type: string
          format: date-time
      updatedAt:
          type: string
          format: date-time
  courseBody:
    type: object
    properties:
      title:
        type: string
      description:
        type: string
      author:
        type: string
        ref: '#components/user'
      lessons:
        type: array
        items:
          type: string
          ref: '#components/lesson'
      attachments:
        type: array
        items:
          type: string
    required:
      - title
      - description