definitions:
  categories:
    type: array
    items:
      type: object
      properties:
        _id:
          type: string
        name:
          type: string
        categoryIcon:
          type: object
          properties:
            path:
              type: string
            color:
              type: string
        subjectsIcon:
          type: string
        totalOffers:
          type: object
          properties:
            student:
              type: number
              default: 0
              $ref: '#/definitions/offer'
            tutor:
              type: number
              default: 0
              $ref: '#/definitions/offer'
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
  categoryBody:
    type: object
    properties:
      name:
        type: string
      categoryIcon:
        type: object
        properties:
          path:
            type: string
          color:
            type: string
      subjectsIcon:
        type: string
  category:
    type: object
    properties:
      _id:
        type: string
      name:
        type: string
      categoryIcon:
        type: object
        properties:
          path:
            type: string
          color:
            type: string
      subjectsIcon:
        type: string
      totalOffers:
        type: object
        properties:
          student:
            type: number
            default: 0
            $ref: '#/definitions/offer'
          tutor:
            type: number
            default: 0
            $ref: '#/definitions/offer'
      createdAt:
        type: string
        format: date-time
      updatedAt:
        type: string
        format: date-time
