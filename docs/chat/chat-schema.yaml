definitions:
  chats:
    type: array
    items:
      type: object
      properties:
        _id:
          type: string
        members:
          type: object
          properties:
            user:
              type: object
              $ref: '#/components/user'
            role:
              type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        deletedFor:
          type: array
          items:
            type: object
            properties:
              user:
                type: object
                $ref: '#/components/user'
        latestMessage:
          type: string
          $ref: '#/definitions/message'
  chatBody:
    type: object
    properties:
      member:
        type: string
      memberRole:
        type: string
  chat:
    type: object
    properties:
      _id:
        type: string
      members:
        type: object
        properties:
          user:
            type: object
            ref: '#/components/user'
          role:
            type: string
      createdAt:
        type: string
        format: date-time
      updatedAt:
        type: string
        format: date-time
      deletedFor:
        type: array
        items:
          type: object
          properties:
            user:
              type: object
              $ref: '#/components/user'
      latestMessage:
        type: string
        $ref: '#/definitions/message'
