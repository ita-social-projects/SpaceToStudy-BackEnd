definitions:
  courses:
    type: array
    items:
      type: object
      properties:
        _id:
          type: string
        title:
          type: string
        description:
          type: string
        author:
          type: string
          ref: '#components/user'
        category:
          type: string
          ref: '#components/category'
        subject:
          type: string
          ref: '#components/subject'
        proficiencyLevel:
          type: array
          items:
            type: string
            enum:
              - Beginner
              - Intermediate
              - Advanced
              - Test Preparation
              - Professional
              - Specialized
        sections:
          type: array
          items:
            type: object
            properties:
              title:
                type: string
              description:
                type: string
              lessons:
                type: array
                items:
                  type: string
                  ref: '#/components/lesson'
              quizzes:
                type: array
                items:
                  type: string
                  ref: '#/components/quiz'
              attachments:
                type: array
                items:
                  type: string
                  ref: '#/components/attachments'
              order:
                type: array
                items:
                  type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
  course:
    type: object
    properties:
      _id:
        type: string
      title:
        type: string
      description:
        type: string
      author:
        type: string
        ref: '#components/user'
      category:
        type: string
        ref: '#components/category'
      subject:
        type: string
        ref: '#components/subject'
      proficiencyLevel:
        type: array
        items:
          type: string
          enum:
            - Beginner
            - Intermediate
            - Advanced
            - Test Preparation
            - Professional
            - Specialized
      sections:
        type: array
        items:
          type: object
          properties:
            title:
              type: string
            description:
              type: string
            lessons:
              type: array
              items:
                type: string
                ref: '#/components/lesson'
            quizzes:
              type: array
              items:
                type: string
                ref: '#/components/quiz'
            attachments:
              type: array
              items:
                type: string
                ref: '#/components/attachments'
            order:
              type: array
              items:
                type: string
      createdAt:
        type: string
        format: date-time
      updatedAt:
        type: string
        format: date-time
  courseBody:
    type: object
    properties:
      title:
        type: string
      description:
        type: string
      author:
        type: string
        ref: '#components/user'
      category:
        type: string
        ref: '#components/category'
      subject:
        type: string
        ref: '#components/subject'
      proficiencyLevel:
        type: array
        items:
          type: string
          enum:
            - Beginner
            - Intermediate
            - Advanced
            - Test Preparation
            - Professional
            - Specialized
      sections:
        type: array
        items:
          type: object
          properties:
            title:
              type: string
            description:
              type: string
            lessons:
              type: array
              items:
                type: string
                ref: '#/components/lesson'
            quizzes:
              type: array
              items:
                type: string
                ref: '#/components/quiz'
            attachments:
              type: array
              items:
                type: string
                ref: '#/components/attachments'
            order:
              type: array
              items:
                type: string
    required:
      - title
      - description
