definitions:
  notes:
    type: array
    items:
      type: object
      properties:
        _id:
          type: string
        text:
          type: string
        author:
          type: string
          ref: '#components/user'
        isPrivate:
          type: boolean
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
  noteBody:
    type: object
    properties:
      text:
        type: string
      isPrivate:
        type: boolean
  note:
    type: object
    properties:
      _id:
        type: string
      text:
        type: string
      author:
        type: string
        ref: '#components/user'
      isPrivate:
        type: boolean
      createdAt:
        type: string
        format: date-time
      updatedAt:
        type: string
        format: date-time
