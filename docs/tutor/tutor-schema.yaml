definitions:
  tutors:
    type: array
    items:
      type: object
    properties:
      _id:
        type: string
      role:
        type: string
        enum: [student, tutor]
      firstName:
        type: string
      lastName:
        type: string
      email:
        type: string
      photo:
        type: string
      biography:
        type: string
      city:
        type: string
      education:
        type: string
      categories:
        #TODO: add ref
        type: string
      averageRating:
        type: number
      ratingQuantity:
        type: number
      isEmailConfirmed:
        type: boolean
      isFirstLogin:
        type: boolean
      lastLogin:
        type: string
        format: date-time
      active:
        type: boolean
      blocked:
        type: boolean
      languagesSpoken:
        type: array
        items:
          type: object
          properties:
            language:
              type: string
            level:
              type: string
              enum: [Beginner, Intermediate, Advanced, Test Preparation, Professional, Specialized]
  tutor:
    type: object
    properties:
      _id:
        type: string
      role:
        type: string
        enum: [student, tutor]
      firstName:
        type: string
      lastName:
        type: string
      email:
        type: string
      photo:
        type: string
      biography:
        type: string
      city:
        type: string
      education:
        type: string
      categories:
        #TODO: add ref
        type: string
      averageRating:
        type: number
      ratingQuantity:
        type: number
      isEmailConfirmed:
        type: boolean
      isFirstLogin:
        type: boolean
      lastLogin:
        type: string
        format: date-time
      active:
        type: boolean
      blocked:
        type: boolean
      languagesSpoken:
        type: array
        items:
          type: object
          properties:
            language:
              type: string
            level:
              type: string
              enum: [Beginner, Intermediate, Advanced, Test Preparation, Professional, Specialized]
