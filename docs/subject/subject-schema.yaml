definitions:
  subjects:
    type: array
    items:
      type: object
      properties:
        _id:
          type: string
        name:
          type: string
        price:
          type: number
        proficiencyLevel:
          type: string
          enum: [Beginner, Pre-Intermediate, Intermediate, Upper-Intermediate, Advanced, Native]
        category:
          type: string
          $ref: '#/components/category'
  subjectBody:
    type: object
    properties:
      name:
        type: number
      price:
        type: number
      proficiencyLevel:
        type: string
        enum: [Beginner, Pre-Intermediate, Intermediate, Upper-Intermediate, Advanced, Native]
      category:
        type: string
        $ref: '#/components/category'
  subject:
    type: object
    properties:
      _id:
        type: string
      name:
        type: number
      price:
        type: number
      proficiencyLevel:
        type: string
        enum: [Beginner, Pre-Intermediate, Intermediate, Upper-Intermediate, Advanced, Native]
      category:
        type: string
        $ref: '#/components/category'
