definitions:
  quizzes:
    type: array
    items:
      type: object
      properties:
        _id:
          type: string
        title:
          type: string
        questions:
            type: array
            items:
              type: object
              properties:
                text:
                  type: string
                answers:
                  type: array
                  items:
                    type: object
                    text: string
                    correct: boolean
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
  quizBody:
    type: object
    properties:
      title:
        type: string
      questions:
          type: array
          items:
            type: object
            properties:
              text:
                type: string
              answers:
                type: array
                items:
                  type: object
                  text: string
                  correct: boolean
  quiz:
    type: object
    properties:
      _id:
        type: string
      title:
        type: string
      questions:
          type: array
          items:
            type: object
            properties:
              text:
                type: string
              answers:
                type: array
                items:
                  type: object
                  text: string
                  correct: boolean
      createdAt:
        type: string
        format: date-time
      updatedAt:
        type: string
        format: date-time