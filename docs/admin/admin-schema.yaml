definitions:
  admins:
    type: array
    items:
      type: object
      properties:
        _id:
          type: string
        firstName:
          type: string
        lastName:
          type: string
        email:
          type: string
        active:
          type: boolean
        blocked:
          type: boolean
        signUpDate:
          type: string
          format: date-time
        lastLogin:
          type: string
          format: date-time
  admin:
    type: object
    properties:
      _id:
        type: string
      firstName:
        type: string
      lastName:
        type: string
      email:
        type: string
      active:
        type: boolean
      blocked:
        type: boolean
      signUpDate:
        type: string
        format: date-time
      lastLogin:
        type: string
        format: date-time
      
