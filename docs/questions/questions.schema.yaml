definitions:
  questions:
    type: array
    items:
      type: object
      properties:
        _id:
          type: string
        title:
          type: string
        text:
          type: string
        answers:
          type: array
          items:
            type: object
            properties:
              text:
                type: string
              isCorrect:
                type: boolean
        category:
          type: string
          ref: '#/definitions/resourcesCategory'
        author:
          type: string
          ref: '#/components/user'
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
  questionsBody:
    type: object
    properties:
      title:
        type: string
      text:
        type: string
      answers:
        type: array
        items:
          type: object
          properties:
            text:
              type: string
            isCorrect:
              type: boolean
      category:
        type: string
        ref: '#/definitions/resourcesCategory'
  question:
    type: object
    properties:
      _id:
        type: string
      title:
        type: string
      text:
        type: string
      answers:
        type: array
        items:
          type: object
          properties:
            text:
              type: string
            isCorrect:
              type: boolean
      category:
        type: string
        ref: '#/definitions/resourcesCategory'
      author:
        type: string
        ref: '#/components/user'
      createdAt:
        type: string
        format: date-time
      updatedAt:
        type: string
        format: date-time
